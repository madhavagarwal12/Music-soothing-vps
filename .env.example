# Host port to publish AuraFlow on (container always listens on 8080 internally)
HOST_PORT=8080

# Only used by docker-compose.prod.yml on the VPS — the GHCR image to pull.
# Replace OWNER/REPO with your actual GitHub org/user and repo name.
IMAGE=ghcr.io/OWNER/REPO:latest
